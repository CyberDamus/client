# Database (Vercel Postgres)
#
# RECOMMENDED: Get automatically from Vercel
#   1. Create Postgres database in Vercel Dashboard (Storage → Create → Postgres)
#   2. Pull env vars locally:
#      vercel link
#      vercel env pull .env.local
#
# ALTERNATIVE: Manual setup (if not using Vercel)
#   Get connection string from: https://console.neon.tech
#   DATABASE_URL="postgresql://user:password@ep-xxx.neon.tech/cyberdamus?sslmode=require"
#
# After DATABASE_URL is set, run:
#   npx prisma generate
#   npm run db:push

DATABASE_URL="postgresql://..."
